{
  "name": "ecoroute-sync-backend",
  "version": "1.0.0",
  "description": "Backend logic for EcoRoute Sync",
  "main": "src/handlers/task-handler.ts",
  "scripts": {
    "build": "tsc",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "dependencies": {
    "drizzle-orm": "latest",
    "pg": "latest"
  },
  "devDependencies": {
    "@types/node": "^20.0.0",
    "@types/pg": "^8.0.0",
    "typescript": "^5.0.0"
  }
}
